# 声明与品牌边界 · NOTICE

本仓库以 **GNU Affero General Public License v3.0（AGPL-3.0-only）** 发布，全文见 [LICENSE](./LICENSE)。

AGPL 授予你对**源代码**的广泛自由：使用、修改、再分发，包括商业用途。同时它带有一条对网络服务特别重要的条款：

> **§13 Remote Network Interaction** —— 如果你修改了本程序并通过网络向用户提供服务，你必须向这些用户提供你修改后的完整源代码。

这条正是本项目选择 AGPL 而非 MIT/Apache 的原因：它让「拿开源代码改个名字就做成闭源 SaaS」这条路走不通。你当然可以商用，但改了就得公开。

---

## 一、AGPL 覆盖什么

| 覆盖 | 说明 |
|---|---|
| 本仓库内的全部源代码 | TypeScript / TSX / CSS / 构建配置 |
| 你的修改版本 | 必须同样以 AGPL-3.0 发布 |
| 你基于它提供的网络服务 | 需向使用者提供修改后的源码（§13） |

## 二、AGPL 不覆盖什么

**版权许可 ≠ 商标许可。** 以下内容不在 AGPL 授权范围内，未经书面许可不得使用：

| 不覆盖 | 说明 |
|---|---|
| **「ZGo」名称与标识** | 名称、Logo（`public/brand-mark.png`、`src/components/Logo.tsx`）为项目方标识 |
| **线上服务** | 项目方运营的线上托管实例 `myzgo.cn`，及其运营主体、备案信息、支付账号 |
| **用户数据** | 线上服务收集/存储的任何用户与账单数据 |
| **内部文档** | 商业策略、定价方案、推广材料、部署与运维文档（不在本仓库中） |

也就是说：**你可以用这份代码，但不能用这个名字、这个 Logo，也不代表你可以运营这套线上服务。**

> **关于 `myzgo.cn`：** 你会在 README 和页脚看到这个地址 —— 它是项目方运营的官方实例，
> 由本仓库代码部署而来，在此**作为「不想自己部署时的现成入口」公开**。
> 这不构成对域名、经营主体或该服务的任何授权：fork 后请按[第三节](#三fork-检查清单)
> 把 `site.ts` 里的 `HOSTED_URL` 改掉或关掉 `SHOW_HOSTED_LINK`，
> 不要让你的分支把用户引到项目方的实例上。

---

## 三、Fork 检查清单

如果你打算基于本仓库部署一个自己的服务，请至少完成以下替换：

- [ ] **换名字**：不要沿用「ZGo」，`package.json` 的 `name`、页面标题、页脚一并改
- [ ] **换标识**：替换 `public/brand-mark.png` 与 `src/components/Logo.tsx`
- [ ] **填主体信息**：`src/lib/site.ts` 中的 `BUSINESS_ENTITY`、`CONTACT_EMAIL`、`ICP_NUMBER` 目前是占位符，需替换为你自己的
- [ ] **关掉官方实例入口**：把 `src/lib/site.ts` 的 `SHOW_HOSTED_LINK` 设为 `false`（或改掉 `HOSTED_URL`），避免把用户引向上游实例
- [ ] **保留许可证**：以 AGPL-3.0 发布你的修改版，并保留本文件与 `LICENSE`
- [ ] **保留版权声明**：源码中的版权头不要删除

> `src/lib/site.ts` 里的 `GITHUB_URL` 指向上游仓库，若你的 fork 有独立仓库请自行修改。

---

## 四、免责声明

本项目是一个**接口健康检测工具**，用于帮你判断自己的 API 服务商是否稳定可用。它按「现状」提供，不附带任何担保。检测结果反映的是生成时刻的单次观测，**不构成对任何服务商的质量承诺或推荐**。

使用前请确认：你拥有对所检测接口的合法访问权限。检测会向目标接口发送真实请求，可能消耗你的账户余额。

---

*如果你在自己的项目里用到了这份代码，欢迎在 README 里提一句来源 —— 不是义务，是礼貌。*
